const { app } = require('electron');
console.log('App is:', app ? 'Defined' : 'Undefined');
const puppeteer = require('puppeteer');
console.log('Puppeteer required');
console.log('App is:', app ? 'Defined' : 'Undefined');
